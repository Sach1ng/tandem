# Pip browser bridge (local-only HTTP server the extension calls).
TANDEM_BRIDGE_PORT=8765
# Working dir the agent runs in. Defaults to the Pip repo root (AGENTS.md + PM OS submodule).
CURSOR_WORKDIR=
CURSOR_BIN=cursor-agent
CURSOR_MODEL=auto
