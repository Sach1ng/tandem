{
  "name": "@tandem/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tandem in the browser — ambient, contextual prompts about whatever page you're on, answered by the Cursor CLI against the PM OS workspace.",
  "scripts": {
    "build": "node build.mjs",
    "bridge": "node --env-file=.env --import tsx bridge/server.ts"
  },
  "dependencies": {
    "@tandem/core": "*",
    "@tandem/engine": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287"
  }
}
