{
  "name": "@tandem/chrome-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lens — Tandem's page-aware Chrome extension. Ambient prompts about whatever page you're on.",
  "scripts": {
    "build": "node build.mjs",
    "bridge": "node --env-file=.env --import tsx bridge/server.ts"
  },
  "dependencies": {
    "@tandem/core": "*",
    "@tandem/engine": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287"
  }
}
